Версія 5.28 (жовтень 2022 р.)
=============================

Додано дію "Плаваючий текст".

Додано "Робочі змінні" до блоків дій.

Дія Блокування дії тепер підтримує опцію блокування наступних дій до завершення (увімкнуто за замовчуванням).

Додано дію "Очистити словник/масив", щоб дозволити окремі статті очищатися або видалятися.

Додано підтримку автоматизованих перекладів у режимі реального часу в Магазині шаблонів.

Додана підтримка супутникового перегляду в екрані налаштування геозони.

Додано магічний текст для сили сигналу вежі стільникового зв'язку.

Покращено пошук на екрані списку блоків дій для пошуку дій у блоках.

Вирішено проблему, через яку розділення текстової маніпуляції на масив не працювало з кількома роздільниками символів.

Додано нові налаштування в параметрах тригера для налаштування часу завершення тригера кнопки живлення.

Інші незначні виправлення та покращення.

Версія 5.27.8 (вересень 2022 р.)
===============================

Невеликі помилки виправляють випуск, щоб виправити ряд незначних проблем.

Версія 5.27 (вересень 2022 р.)
=============================

Додано дію служби спеціальних можливостей (потрібен злам ADB).

Додана функціональність для підтримки служб спеціальних можливостей для вирішення проблем, через які деякі пристрої можуть їх вимкнути (потрібен ADB Hack).

Додано обмеження настройок системи.

Додано загальні та доступні значення оперативної пам'яті до магічних текстових опцій.

Додані числові показники для значень висоти тону і швидкості в тексті до дії мовлення.

Додано обхідний шлях для проблеми з запускачем Samsung, щоб запобігти втраті ярликами піктограми/мітки під час увімкнення/вимкнення темного режиму.

Додано магічний текст для назви мережі SIM/оператора.

Додано магічний текст для типу з'єднання з клітинками (наприклад, 3G/ 4G / 5G).

Доданий магічний текст для MacroDroid є "за" (повертає значення true або false)

Додано підтримку параметра "Цей макрос" до обмеження макросів увімкнуто/вимкнено.

Додана опція підтримки пробілів для дії спливаючого повідомлення.

Додано підтримку перейменування секундомірів.

Додано опцію для відображення останнього відредагованого часу на екрані списку макросів (Усі значення спочатку будуть невідомі, доки їх не буде відредаговано в цій останній версії).

Дія "Встановити змінну" (за допомогою параметра "Нова змінна") тепер створить змінну під час виконання та підтримує магічний текст для імені змінної.

Супутня програма Wear OS тепер дозволяє прокручувати список за допомогою поворотного керування.

Покращення та виправлення щодо обробки словників/масивів у тригері та обмеженні змінних.

Виправлено проблему, коли задана змінна дії могла в деяких ситуаціях створювати словник замість масиву.

Вирішено проблему, через яку змінну локального масиву/словника не вдалося перейменувати.

Вирішено проблему, через яку перегляд шаблону з блоками дій міг додати небажані глобальні змінні.

Вирішено проблему, через яку видалення статті зі словника/масиву блоку дій могло б видалити словник/масив.

Версія 5.26 (липень 2022 р.)
========================

Дії "Запустити макрос", "Скасувати макрос" і "Вимкнути макрос" тепер підтримують введення макросу за іменем (підтримується магічний текст, що дозволяє посилатися на імена макросів зі змінної).

Додаткові налаштування, такі як розташування заходу/сходу сонця та розташування погоди, тепер зберігаються під час експорту та за допомогою автоматичного резервного копіювання.

Обмеження змінної MacroDroid тепер підтримує параметр визначення ключів вручну для змінних словника/масиву.

Діалогове вікно параметрів тепер підтримує базове форматування HTML в основному тексті повідомлення.

Вирішено проблему, через яку категорії не можна було додати або видалити з екрана списку категорій.

Вирішено проблему в тригерах вхідних SMS/дзвінків, коли порівняння номерів не завжди працювало коректно при використанні магічного тексту.

Вирішено проблему, через яку запуск ярлика MacroDroid зводив би до мінімуму MacroDroid, якщо він знаходиться на передньому плані.

Вирішено проблему, через яку блок дій міг дублюватися під час імпорту макросу, який уже посилається на той самий блок дій.

Вирішено проблему, через яку блоки дій могли бути видалені під час тестування певних дій.

Виправлено проблему, через яку розрив циклу не завжди працював належним чином із ітераційною дією словника/масиву.

Вирішено проблему, через яку пошук піктограм не працював належним чином для пакетів піктограм.

Вирішено проблему, через яку настройка плаваючої кнопки не дозволяла вибирати тригери в дії «Зачекати до тригера».

Інші незначні виправлення.

Версія 5.25.6 (липень 2022 р.)
===============================

Невелика помилка виправляє оновлення до попереднього випуску v5.25. Виправляє ряд незначних проблем, виявлених у попередньому випуску.

Версія 5.25 (червень 2022 р.)
========================

Додано тригер увімкнення макросу.

Додано опцію до тригера з плаваючою кнопкою, щоб вимкнути тригер (не весь макрос) при видаленні через значок bin.

Додано дію налаштування плаваючої кнопки.

Оновлено магічний текст для підтримки фігурних дужок, щоб зменшити плутанину з синтаксисом словника/масиву. (Квадратні дужки все ще підтримуються, {system_time} і [system_time] як працюють, так і за замовчуванням можна змінити в налаштуваннях MacroDroid)

Додано кращу підтримку масивів/словників для таких дій, як HTTP-запит та читання файлів, щоб дозволити пряме записування на власні ключі.

Додано опцію "Розділити на масив" до дії "Маніпулювання текстом".

Налаштований тригер Календаря, щоб краще працювати з подіями на весь день у різних часових поясах.

При перегляді макросу шаблону доступний новий пункт меню "Додати вимкнено".

Голосове введення тепер підтримує налаштування безпосередньо в нові словникові ключі/індекси масивів з магічним текстом.

Дія спливаючого повідомлення тепер підтримує основні html, такі як <b>BOLD</b> та <i>ITALIC</i>

Додано увімкнення/вимкнення (включаючи дітей) для редагування екрана макросів, що полегшує створення великих умов і циклів.

Додано опцію ввімкнення/вимкнення горизонтальних свайпів для відкриття шухляди MacroDroid.

Додано опцію «Перезаписати» для дії «Записати у файл».

Додана опція WhatsApp Send action для підтримки як стандартного, так і бізнес-додатка.

Додано магічний текст для розміру словника/масиву.

Вирішено проблему, через яку використання вирізання на блоці IF/Loop не працювало належним чином.

Вирішено проблему, через яку параметр діалогового вікна параметра "Продовжити" не працював належним чином.

Вирішено проблему, через яку під час запису мікрофона дія не вела правильне розширення файлу.

Вирішено проблему, через яку значення змінної не оновлювалося, якщо його оновлювати під час відкриття шухляди MacroDroid.

Вирішено проблему, через яку редагування вимкнутого макросу могло знову ввімкнути його.

Вирішено проблему, через яку сповіщення про відображення не проходило б змінні належним чином у параметрі "Виконати" при натисканні.

Додана опція в налаштуванні відновлення всіх карток довідкової інформації, які раніше були затемнені.

Версія 5.24.3 (червень 2022 р.)
==========================

Це оновлення було потрібно Google, щоб додати службу розпізнавання під час використання MacroDroid як помічника за замовчуванням.

Версія 5.24 (травень 2022 р.)
==========================

Додано дію "Читання вмісту екрана".

Додано тригер вмісту екрана.

Додано дію "Читання з файлу".

Додано обмеження категорії Ввімкнуто/Вимкнуто.

Додано дію Експортувати журнал.

Додана можливість вручну вводити ідентифікатор перегляду в кліках взаємодії інтерфейсу користувача.

Додано параметр "Встановити гучність" для пристроїв, які не дозволяють встановлювати гучність за допомогою MacroDroid у фоновому режимі.

Додано параметр дії "Спливаюче повідомлення" для скасування попереднього спливаючого вікна (наприклад, корисно для другого зворотного відліку)

Додано параметр до дій діалогового вікна повідомлення та діалогового вікна параметрів, щоб запобігти закриттю кнопки «Назад».

Додано підтримку в тригері WebHook для налаштування статей змінних словника/масиву.

Функція повідомлення про помилку тепер включає можливість додавати скріншоти.

Додано підтримку перейменування ярликів у шухляді MacroDroid.

Панель макродроїдів тепер підтримує параметри відкриття за допомогою свайпа вгору та/або жесту вниз.

Інші незначні виправлення та налаштування.

Версія 5.23 (квітень 2022 р.)
==========================

Додано типи змінних словника та масиву (лише для користувачів Pro).

Додано дію "Ітерувати словник/масив", яку можна використовувати для циклу всіх елементів у словнику/масиві.

Додано дію JSON Parse.

Змінено дію "Установити швидкий стан плитки" на "Настроїти швидкий сегмент", щоб дозволити додаткове настроювання піктограми та підпису.

Додано опцію послідовності жестів до дії взаємодії з інтерфейсом користувача, щоб увімкнути складні жести, такі як шаблони блокування пальців.

Додана опція до екрана конфігурації взаємодії з інтерфейсом користувача, щоб переключити накладання сенсорного розташування.

Додана можливість вмикати/вимикати окремі буфери до тригера Logcat.

Додано обмеження IP-адреси.

Додано чарівний текст для базової URL-адреси WebHook.

Додано функцію пошуку на екрані списку блокування дій.

Додано можливість експорту системного входу в HTML, щоб він міг виводити те саме кольорове кодування, що показано в додатку.

Додано опцію вирізання для редагування екранів макросів і блоків дій, щоб дозволити повну функціональність вирізання, копіювання та вставлення.

Блок дій "Тестові дії" тепер відображатиме зведення вихідних змінних при запуску.

Вирішено проблему, через яку метод виклику макросів не працював належним чином у всіх випадках.

Усунено проблему, через яку умова "не на випадок" не працює належним чином у обмеженні календаря.

Вирішено проблему, через яку ім'я файлу в дії HTTP Request не обробляло належним чином значення тексту.

Вирішено проблему, через яку дії в блоці дій не скасовувалися після скасування батьківського макросу.

Вирішено проблему, через яку на деяких пристроях можна було неправильно повертати шпалери.

Інші незначні виправлення та налаштування.

Версія 5.22.6 (лютий 2022 р.)
============================

Вирішено проблему, через яку відео MacroDroid не працювало.

Версія 5.22 (лютий 2022 р.)
============================

Додано дію Http Request.

Додано обмеження методу виклику, щоб дозволити виявлення, коли макрос викликається з нестандартних тригерів макросів.

Додано базову (лише англійською) функцію пошуку до засобу вибору піктограм.

Додано підтримку блокування дій у шухляді macroDroid.

Додано підтримку біометрії та запит пароля затримки в налаштуваннях захисту паролем.

Додано магічний текст для номера версії MacroDroid.

Додано параметр "Ігнорувати регістр" до тригера та обмеження змінної.

Додано підтримку передачі значень булевих змінних у вхідні параметри блоку дії.

Додано параметр "Ігнорувати " (Скасувати макрос) для діалогового вікна параметрів, щоб запобігти виконанню будь-яких подальших дій, якщо вибрано.

Додана можливість фільтрувати змінні за типами на екрані глобальних змінних.

Додано опцію виключення тригерів на основі дзвінків/SMS та додано імена контактів із можливістю пошуку до діалогового вікна вибору.

Усунено проблему, через яку збирання значень масиву Задачера в один рядок іноді призводило до неправильного порядку елементів.

Вирішено проблему, через яку спільний доступ до найближчого пристрою не завжди працював на пристроях Android 12.

Виправлено проблему, коли вибір глобальної змінної для дії "Голосовий вихід" міг встановити неправильну змінну.

Вирішено проблему, через яку тригер маяка Bluetooth міг спрацьовувати кілька разів для однієї події.

Вирішено проблему, через яку geofence не зосереджувався на правильній області під час пошуку або збільшення/зменшення масштабу.

Вирішено проблему, через яку список SSID Wi-Fi міг містити повторювані записи.

Вирішено проблему, через яку дуже довгий рядок у локальній змінній міг перешкоджати доступності кнопки OK.

Вирішено проблему, через яку піктограма Set MacroDroid не могла оновити піктограму в деяких сценаріях.

Вирішено проблему, через яку події на весь день у календарному обмеженні не працювали належним чином у різних часових поясах.

Багато інших незначних виправлень і налаштувань.

Версія 5.21
============

Випуск виправлень дрібних помилок.

Налаштовані дії блокують обробку вхідних параметрів, щоб ви могли передавати порожнє значення і завжди використовувати значення вхідної парми за замовчуванням.

Додано опцію "Поділитися як зображенням" на екрані редагування "Блок дії".

Виправлено кілька невеликих проблем, пов'язаних з редагуванням блоку дій.

Вирішено проблему, через яку безкоштовні користувачі могли втратити макроси, якщо вони мають обмеження в 5 макросів і відхиляють редагування макросу або блоку дій.

Вирішено проблему з тим, що плитка "Останнє відкриття" на головному екрані в деяких ситуаціях не працювала.

Усунено проблему з дією "Голосовий вхід", яка не спричиняє загоряння тригера змінної під час запису до змінної.

Усунено проблему, через яку вставлення елементів у блок дій не завжди налаштовувало дії належним чином.

Версія 5.20
============

Додано блоки дій, щоб дозволити групування часто використовуваних дій, які можна викликати з макросів або інших блоків дій.

Додано підтримку спільного доступу до найближчих пристроїв із екрана редагування макросів і блокування дій.

Додано дію "Встановити зображення панелі кнопок", щоб дозволити оновлення кнопок за допомогою панелі сповіщень MacroDroid.

Додано дію голосового вводу, щоб надіслати запит на вимовлений текст, а потім зберегти його в рядковій змінній.

Додано дію WhatsApp (бета-версія), щоб дозволити базову розсилку повідомлень лише окремим контактам.

Додано можливість позначати макроси як улюблені та легко фільтрувати між улюбленими/всіма макросами.

Додано плитку головного екрана для надання швидкого сполучення клавіш для вибраних макросів.

Додано плитку головного екрана для відео MacroDroid, яка містить посилання на нові офіційні відео YouTube MacroDroid.

На екрані перекладів тепер показано, наскільки повний переклад для кожної мови.

Виправлені різні незначні помилки та невеликі покращення.

Версія 5.19.11
===============

Вирішено проблему, через яку запит пароля не з'являвся в деяких випадках при використанні захисту паролем для входу в додаток.

Вирішено проблему, через яку спільний доступ до останньої фотографії працював не у всіх випадках.

Усунено проблему, через яку тригер сну не перевіряв обмеження.

Виправлено інші незначні помилки.

Версія 5.19.9
==============

Зверніть увагу, що цей випуск оновлено, щоб орієнтуватися на Android 11 SDK, який забезпечує більш жорсткі обмеження на доступ до файлів, ніж попередні версії. Невелика кількість макросів може потребувати оновлення для продовження запису у файли.

** Будь ласка, повідомляйте про будь-які помилки або проблеми з цим випуском через "Повідомити про помилку" в розділі усунення несправностей **

Додано тригер сну (Android 10+).

Додано дію "Очистити змінні".

Додано опцію logcat для підтримки тригера буфера обміну для користувачів Android 10+ (злом Root/ADB).

Додано дію Оновлення буфера обміну, щоб отримати вміст буфера обміну в MacroDroid для користувачів Android 10+.

Додано підтримку до трьох кнопок дій до дії Сповіщення про відображення, які можуть запускати інші макроси після натискання.

Вирішено проблему, через яку ящик не відображався належним чином на пристрої з макетами справа наліво.

Покращено продуктивність, коли додано/видалено кілька плаваючих кнопок.

Send UDP action тепер підтримує магічний текст для полів призначення та порту.

Додано опцію "Вийти назад" для "Якщо потім підтвердити дію".

Додано опцію "Дозволити будь-який сертифікат" до HTTP Get.

Додано підтримку для надсилання електронної пошти через SMTP до дії Share Location (Поділитися розташуванням).

Додано підтримку запуску команд сценарію оболонки через файл-помічник (Потрібен файл-помічник 1.7)

Додано обмежену підтримку доступу до значень масиву Tasker (плагін/дія Tasker) шляхом збереження всіх значень у комі або новому рядку, розділеному між рядками.

Виправлення до попереднього оновлення: Велике спасибі lamvankhoat1 за ретельний в'єтнамський переклад.

Версія 5.18
============

Випуск виправлень незначних помилок

Додано перемикач для ввімкнення/вимкнення режиму надсилання HTML на Send Email (через SMTP).

Вирішено проблему, через яку піктограму не можна було вибрати під час дії сповіщення.

Вирішено проблему, через яку дія Take ScreenShot надсилала неправильне зображення на деякі пристрої.

Вирішено проблему, через яку дія "Якщо підтверджено" не працювала належним чином, якщо її викликати під час вимкнення екрана.

Вирішено проблему, через яку деякі значення жестів взаємодії інтерфейсу користувача не перетворювали правильне значення.

Вирішено проблему, через яку ви не могли редагувати дані в дії НАДІСЛАТИ SMS після її налаштування.

Інші невеликі виправлення. W

Версія 5.17
============

Додано тригер зміни темної теми (Android 10+)

Змінено текст макроопису на екрані редагування макросу, щоб він прокручувався з макровмістом, і розширювалася кількість дозволених символів.

Додано підтримку змінних у дії жестів взаємодії інтерфейсу користувача.

Додано підтримку введення пароля в дії Set Variable - запит користувача.

Вирішено проблему, через яку функція ідентифікації взаємодії інтерфейсу користувача в параметрі програми не працювала на деяких пристроях.

Додано в'єтнамську мову (Велике спасибі lamvankhoat1 за завершення цього)

Додана можливість змінювати текст ярликів додатків в макродроїдної шухляді.

Багато інших невеликих виправлень і налаштувань.

Версія 5.16.2
==============

Вирішено проблему, через яку програма запускала тригер і виявляла, що програма переднього плану не працювала на деяких пристроях.

Інші невеликі виправлення.

Версія 5.16
============

Вирішено проблему, через яку деякі макроси могли бути відсутніми у списку шаблонів.

Виправлено багато незначних помилок.

Версія 5.15
============

Відновлена опція інвертованого порядку до системного журналу.

Додано можливість захоплення повідомлень Logcat (і вибору запису) до тригера Logcat.

Додано посилання на вікі в бічному меню (Вікі лише англійською мовою).

Додано підтримку підтексту в тригері сповіщень (і пов'язаного з ним магічного тексту).

Усунено проблему, через яку плаваючі кнопки не зберегли своє попереднє положення.

Вирішено проблему, через яку відкритий системний журнал не працював належним чином.

Вирішено проблему, через яку вкладення системного журналу для надсилання дії електронної пошти не працювало належним чином.

Багато інших невеликих виправлень і налаштувань.

Версія 5.14
============

Додано дію «Відновити приховані сповіщення» (Android 11+), щоб дозволити відновити постійні сповіщення, які раніше були приховані за допомогою дії «Очистити сповіщення».

Вирішено проблему, через яку в системному журналі не відображалися посилання на макроси.

Інші невеликі виправлення і поліпшення.

Версія 5.13
============

Новий системний журнал з функціями пошуку та фільтрації.

Додано підтримку перегляду журналу для одного макросу лише зі списку макросів і редагування екрана макросів.

Відновлений тригер надсилання SMS- повідомлень. (MacroDroid тепер дозволено використовувати необхідний дозвіл)

Відновлено опцію «монітор вхідних повідомлень» на тригері «Sms-повідомлення». (MacroDroid тепер дозволено використовувати необхідний дозвіл)

Play Sound action тепер підтримує налаштування аудіоканалу для окремої дії.

Вирішено проблему, через яку дія «Зачекати до тригера» могла неправильно викликати інші блоки, які не слід використовувати.

Додано підтримку магічного тексту до дії «Налаштування системи».

Дані швидкого виконання макросів тепер включаються під час експорту/імпорту макросів.

Тригери "Пропущений виклик" і "Активний виклик" тепер підтримують введення прямого номера та вибір групи.

Категорії та макроси у списку макросів тепер підтримують власні кольори, щоб дозволити встановити будь-який колір.

Вирішено проблему, через яку деякі макроси в сховищі шаблонів не вдалося імпортувати.

Версія 5.12
============

Додано обмеження геозони (розташування).

Додано тригер Google Assistant, щоб ви могли легко визначити, чи був макрос викликаний помічником (а не якимось іншим тригером).

Цикли та дії з умовами з дочірніми діями тепер можна згортати/розширювати, щоб спростити редагування великих макросів.

Версія 5.11
============

Тепер включає підтримку запуску макросів через Помічник Google (тільки англійською мовою в даний час). Переконайтеся, що і мову вашого пристрою, і мову Google Assistant настроєно на англійську для використання цієї функції.

Викличте макроси, сказавши: "Ok Google запустити "ім'я макросу" в MacroDroid". Наразі Google підтримує цю функціональність лише для англійської мови США. Я додам підтримку інших локалей і мов, коли Google дозволить.

Додано власні канали сповіщень для використання з дією сповіщення про відображення.

Додано дію "Перерва від циклу".

Додано дію "Продовжити цикл".

Подальші виправлення помилок і невеликі поліпшення.

Версія 5.10
============

Випуск виправлень помилок. Виправляє кілька невеликих проблем і рідкісних збоїв.

Покращено роботу з деякими відсутніми дозволами.

Версія 5.9
===========

Додано функцію хмарного резервного копіювання даних на екрані автоматичного резервного копіювання (лише для користувачів Pro).

Форум MacroDroid перейшов від Tapatalk до більш чистого форуму, що розміщується самостійно.

Виправлено кілька незначних помилок.

Версія 5.8
===========

Для встановлення дії Wi-Fi тепер потрібен файл-помічник на Android 10+ (обмеження Google примусове для додатків).

Додано дію "Зачекати до тригера", щоб дозволити запущеному макросу призупинити роботу, доки певний тригер не запуститься.

Додана можливість блокування категорій паролем/PIN-кодом.

Додана можливість додавання макросів MacroDroid в меню кнопки живлення (тільки для Android 11)

Додано параметр кольору тексту для дії спливаючого повідомлення.

Додано зачекати до завершення опції "Запуск макросу".

Додано параметр кольору тексту та відтінок значка до дії Спливаюче повідомлення.

Дія «Промовити текст» тепер дозволяє встановлювати мову для кожного окремого екземпляра дії.

Дія "Додати запис календаря" тепер підтримує встановлення часу запису відносно значення змінної.

Вирішено проблему з logcat_line, що чарівний текст не працює.

Вирішено проблему, через яку скасування дії макросу не скасовувало дію Зачекати до тригера.

Виправлено деякі логічні проблеми, коли вкладені команди IF/ELSE не будуть поводитися так, як очікувалося, в деяких крайових випадках.

Версія 5.7
===========

Додано список плагінів для виявлення/спільного використання сторонніх програм, сумісних із MacroDroid (плитка головного екрана).

Додано плитку головного екрана для швидкого доступу до останнього відредагованого макросу.

Додано плитку головного екрана для швидкого запуску макросів.

Додано дію увімкнення/вимкнення камери, щоб дозволити вимкнути камеру для всіх програм на пристрої.

Налаштований control Media Action таким чином, що опція кнопки «Імітувати аудіо» краще працює для керування останнім використовуваним медіа-додатком.

Тригер «День/час» тепер дозволяє встановити певне значення секунд.

Додавання функції пошуку до екрана списку змінних.

Версія 5.6
===========

Дію режиму «У літаку» тепер можна виконувати на некоренованому пристрої на Android 7+ (зробивши MacroDroid програмою-помічником за замовчуванням).

Запуск програми тепер підтримує опцію назви пакета (включаючи магічний текст для підтримки змінних тощо).

Значення змінних MacroDroid тепер можна передавати плагінам Tasker, використовуючи стиль форматування Tasker (%varname).

Фон/відтінок панелі сповіщень і параметри SMTP тепер зберігаються під час експорту макросів.

Додано значення напрямку вітру до тригера погоди (і магічного тексту, специфічного для погоди).

Оновлено цілочисельні змінні таким чином, що тепер вони підтримуються довгим типом, щоб утримувати значення > 2,147,483,647.

Інші незначні виправлення та покращення.

Версія 5.5
===========

Додано перемикач швидкої плитки для ввімкнення/вимкнення MacroDroid (Android 7+).

Додано параметр для приховування відомостей про макрос у поданні списку макросів.

Вирішено проблему, через яку тригер VPN не спрацьовує.

Вирішено проблему, через яку тригер кнопки панелі сповіщень не відображав піктограми.

Значні оновлення деяких перекладів.

Інші незначні виправлення та налаштування інтерфейсу користувача.
